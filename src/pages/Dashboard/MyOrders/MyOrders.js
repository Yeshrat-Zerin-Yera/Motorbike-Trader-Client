import React from 'react';
import useTitle from '../../../hooks/useTitle';

const MyOrders = () => {
    // Title
    useTitle('My Orders');

    return (
        <div>
            MyOrders
        </div>
    );
};

export default MyOrders;